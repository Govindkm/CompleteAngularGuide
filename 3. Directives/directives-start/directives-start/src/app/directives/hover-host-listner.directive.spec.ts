import { HoverHostListnerDirective } from "./hover-host-listner.directive";


describe('HoverHostListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverHostListnerDirective();
    expect(directive).toBeTruthy();
  });
});
